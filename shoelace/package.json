{
  "private": true,
  "version": "0.0.0",
  "dependencies": {},
  "devDependencies": {
    "parcel-bundler": "^1.x",
    "parcel-plugin-jsy": "*"
  },
  "scripts": {
    "build": "parcel build index.html",
    "serve": "parcel serve index.html",
    "start": "npm -s run serve"
  }
}
